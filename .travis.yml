language: go

before_install:                                                                 
  - curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh  
  - dep ensure
  
deploy:
  provider: script
  script: bash build-main.sh
  on:
    branch: master
